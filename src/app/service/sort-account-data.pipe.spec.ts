import { SortAccountData} from './sort-account-data.pipe';

describe('SortAccountDataPipe', () => {
  it('create an instance', () => {
    const pipe = new SortAccountData();
    expect(pipe).toBeTruthy();
  });
});
